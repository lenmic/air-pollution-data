# air-pollution-data
This project contains helpers for preprocessing air pollution data for use in machine learning models.

## Configuration

Before using the helpers, you need to twig the configuration file config_sample.ini and save it as config.ini.
